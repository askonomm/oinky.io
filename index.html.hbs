{{>header}}

<div class="landing">

    <h1>Oinky</h1>

    <p>A static site generator that compiles Handlebars templates and Markdown files into a static site. More notably enables the use of a DSL to generate data-sets from Markdown files which can then be used within Handlebars templates.</p>

    <h3>Install</h3>

    <pre><code>curl -s -L https://oinky.io/install.sh | bash -s</code></pre>

</div>

{{>footer}}